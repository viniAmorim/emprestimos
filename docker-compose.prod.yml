# docker-compose.prod.yml
version: "3.7"

services:
  apache:
    volumes:
      # Monta os certificados do Let's Encrypt para app.ucredcredito.com
      # no local que o app.ucredcredito.com.conf espera
      - ${SSL_CERTS_PATH_PROD}:/etc/apache2/ssl_certs_app:ro
